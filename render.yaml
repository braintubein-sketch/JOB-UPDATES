services:
  - type: web
    name: job-updates-portal
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        value: admin@jobupdates.com
      - key: ADMIN_PASSWORD
        value: admin123
      - key: NEXT_PUBLIC_SITE_URL
        value: https://jobupdate.site
      - key: NEXT_PUBLIC_SITE_NAME
        value: JOB UPDATES
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHANNEL_ID
        sync: false
      - key: CRON_SECRET
        value: Raghu@2244

# Cron job to trigger automated scraping every 30 minutes
# Note: Render cron jobs require a paid plan. For free tier, use external cron service.
# Uncomment below if on paid plan:
# crons:
#   - name: job-scraper-cron
#     schedule: "*/30 * * * *"
#     url: https://jobupdate.site/api/cron/fetch?key=Raghu@2244
